import React from "react";

function Home() {
    return(
        <section>
            <div class="container-fluid">
               <h1 className="mt-5">Welcome</h1>
               <p>  </p>
            
            </div>
        </section>
    );
}
// We need cards for
//we need a map
//we need sales trajectories
// we need an activity

export default Home;